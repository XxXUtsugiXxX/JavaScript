for(inicializacion; condicion; incremento) {
    // Bloque de código a ejecutar en cada iteración
}
// Ejemplo de un bucle for que imprime los números del 1 al 5
for(let i = 1; i <= 5; i++) {
    console.log(i);
}

// Bucle while
while(condicion) {
    // Bloque de código a ejecutar mientras la condición sea verdadera
}
// Ejemplo de un bucle while que imprime los números del 1 al 5
let j = 1;
while(j <= 5) {
    console.log(j);
    j++;
}

// Bucle do...while
do {
    // Bloque de código a ejecutar al menos una vez y luego mientras la condición sea verdadera
} while(condicion);
// Ejemplo de un bucle do...while que imprime los números del 1 al 5
let k = 1;
do {
    console.log(k);
    k++;
} while(k <= 5);